apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretly.fullname" . }}
  labels:
    {{- include "secretly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretly.selectorLabels" . | nindent 4 }}